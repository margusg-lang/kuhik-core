# kuhik-core Frontend — Production Environment
# Copy to .env.production and fill with real values

# Backend API URL (must be reachable from browser clients)
NEXT_PUBLIC_API_URL=https://api.example.com